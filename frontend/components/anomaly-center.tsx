"use client";

import Link from "next/link";
import { useState } from "react";
import { AlertTriangle, BrainCircuit, CheckCircle2, ListFilter } from "lucide-react";
import { anomalies, etfs } from "@/lib/demo-data";

export function AnomalyCenter(){const [severity,setSeverity]=useState("");const items=anomalies.filter(a=>!severity||a.severity===severity);return <div className="mx-auto max-w-[1500px]"><div className="flex flex-col justify-between gap-5 lg:flex-row lg:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.16em] text-[var(--accent)]">Anomaly center</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.045em]">Signals with an explanation</h1><p className="mt-2 text-sm text-[var(--muted)]">Deterministic rules lead. ML adds context, never opaque alerts.</p></div><div className="flex items-center gap-2"><ListFilter size={14}/>{["","High","Medium","Low"].map(s=><button key={s||"all"} onClick={()=>setSeverity(s)} className={`rounded-full px-3 py-1.5 text-[10px] ${severity===s?"bg-[var(--ink)] text-[var(--paper)]":"border hairline bg-[var(--surface)]"}`}>{s||"All"}</button>)}</div></div><div className="mt-7 grid gap-4">{items.map(a=>{const etf=etfs.find(e=>e.isin===a.isin);return <article key={a.id} className="rounded-lg border hairline bg-[var(--surface)] p-5"><div className="grid gap-5 lg:grid-cols-[180px_1fr_220px]"><div><span className={`inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-[9px] font-semibold ${a.severity==="High"?"bg-red-50 text-red-700":a.severity==="Medium"?"bg-amber-50 text-amber-700":"bg-slate-100 text-slate-600"}`}><AlertTriangle size={11}/>{a.severity}</span><div className="mt-3 text-[10px] uppercase tracking-wider text-[var(--muted)]">{a.type}</div></div><div><h2 className="text-sm font-semibold">{a.title}</h2><Link href={`/etf/${a.isin}`} className="mt-1 block text-xs text-[var(--accent)]">{etf?.name}</Link><p className="mt-3 max-w-3xl text-xs leading-5 text-[var(--muted)]">{a.explanation}</p><div className="mt-3 flex items-center gap-2 text-[9px] text-[var(--muted)]">{a.type==="Unusual profile"?<BrainCircuit size={12}/>:<CheckCircle2 size={12}/>}Method: {a.type==="Unusual profile"?"Isolation Forest + feature distance":"deterministic quality rule"}</div></div><div className="grid grid-cols-2 gap-2"><div className="rounded-md bg-[var(--paper)] p-3"><div className="text-[9px] uppercase text-[var(--muted)]">Observed</div><div className="mt-2 text-xs font-semibold">{a.observed}</div></div><div className="rounded-md bg-[var(--paper)] p-3"><div className="text-[9px] uppercase text-[var(--muted)]">Expected</div><div className="mt-2 text-xs font-semibold">{a.expected}</div></div></div></div></article>})}</div></div>}
