import Link from "next/link";
import { AppShell } from "@/components/app-shell";
import { ArrowDown, Box, Braces, Database, FileSpreadsheet, GitFork, SearchCode, ServerCog } from "lucide-react";

export default function ArchitecturePage(){const nodes=[[FileSpreadsheet,"Sanitized snapshots","CSV / Parquet"],[SearchCode,"Polars ingestion","Allowlist + normalize"],[Database,"DuckDB layer","Entity + listing views"],[GitFork,"Feature engineering","Quality + vectors"],[ServerCog,"FastAPI","Typed paginated APIs"],[Box,"Next.js product","Interactive analytics"]] as const;return <AppShell><div className="mx-auto max-w-[1300px]"><p className="text-xs font-semibold uppercase tracking-[.16em] text-[var(--accent)]">System architecture</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.045em]">Standalone by design</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-[var(--muted)]">The application consumes sanitized snapshots through a strict public contract. It never imports or calls the original scraper at runtime.</p><div className="mt-8 grid gap-5 xl:grid-cols-[.95fr_1.05fr]"><section className="rounded-lg border hairline bg-[var(--surface)] p-6"><h2 className="text-sm font-semibold">Data and serving path</h2><div className="mt-6 flex flex-col items-center">{nodes.map(([Icon,title,copy],i)=>{const Glyph=Icon;return <div key={title} className="contents"><div className="flex w-full max-w-md items-center gap-4 rounded-lg border hairline bg-[var(--paper)] p-4"><div className="grid size-10 shrink-0 place-items-center rounded-md bg-[var(--accent-soft)] text-[var(--accent)]"><Glyph size={18}/></div><div><div className="text-xs font-semibold">{title}</div><div className="mt-1 text-[10px] text-[var(--muted)]">{copy}</div></div><span className="mono ml-auto text-[9px] text-[var(--muted)]">0{i+1}</span></div>{i<nodes.length-1&&<ArrowDown size={15} className="my-2 text-[var(--muted)]"/>}</div>})}</div></section><div className="space-y-5"><section className="rounded-lg border hairline bg-[var(--surface)] p-6"><div className="flex items-center gap-2"><Braces size={16} className="text-[var(--accent)]"/><h2 className="text-sm font-semibold">Analytical branch</h2></div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-md border hairline p-4"><div className="text-[10px] uppercase text-[var(--muted)]">Similarity</div><div className="mt-2 text-sm font-semibold">Weighted cosine + TF-IDF</div><p className="mt-2 text-xs leading-5 text-[var(--muted)]">Categorical matches, numeric proximity, exchange overlap and text semantics return explicit contributions.</p></div><div className="rounded-md border hairline p-4"><div className="text-[10px] uppercase text-[var(--muted)]">Anomalies</div><div className="mt-2 text-sm font-semibold">Rules + Isolation Forest</div><p className="mt-2 text-xs leading-5 text-[var(--muted)]">Human-readable quality rules take priority; ML highlights unusual multivariate profiles with the leading feature.</p></div></div></section><section className="rounded-lg border hairline bg-[var(--surface)] p-6"><h2 className="text-sm font-semibold">Why this stack</h2><div className="mt-5 divide-y hairline">{[["DuckDB","Fast embedded analytical SQL without a database service."],["Polars","Columnar transforms and Parquet interoperability at low memory cost."],["FastAPI","Typed contracts, validation, OpenAPI, and simple container deployment."],["Next.js","Direct routes, responsive product UI, and cheap frontend hosting."],["Deterministic tools","Reliable numerical answers without shipping the dataset to an LLM."]].map(([k,v])=><div key={k} className="grid gap-2 py-3 sm:grid-cols-[120px_1fr]"><strong className="text-xs">{k}</strong><span className="text-xs leading-5 text-[var(--muted)]">{v}</span></div>)}</div></section><section className="rounded-lg border hairline bg-[var(--accent-soft)] p-6"><h2 className="text-sm font-semibold text-[var(--accent)]">Privacy boundary</h2><p className="mt-2 text-xs leading-5 text-[var(--muted)]">Only allowlisted normalized fields cross the boundary. Source URLs, machine paths, provider adapter keys, raw errors, authentication details, and upstream payloads are excluded.</p><Link href="/dashboard" className="mt-4 inline-block text-xs font-semibold text-[var(--accent)]">Return to command center →</Link></section></div></div></div></AppShell>}
